# studiolab_datadash
StudioLab Usage Dashboard

Simply upload the four files CSV files that contain the usage data: Attendance (tap-ins), Events (the events file from MyPrincetonU), Machine Reservations (the reservations file from the StudioLab manager), and the Trainings file (the specific MPU data for machine trainings). Then, the site will create plots detailing usage of the space.
